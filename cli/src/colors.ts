import colors from 'colors';


export const red = (o: any): string => colors.red(o.toString());
export const green = (o: any): string => colors.green(o.toString());
export const yellow = (o: any): string => colors.yellow(o.toString());
export const blue = (o: any): string => colors.blue(o.toString());
export const magenta = (o: any): string => colors.magenta(o.toString());
export const cyan = (o: any): string => colors.cyan(o.toString());
export const white = (o: any): string => colors.white(o.toString());
export const gray = (o: any): string => colors.gray(o.toString());
export const grey = (o: any): string => colors.grey(o.toString());
export const black = (o: any): string => colors.black(o.toString());
export const bgRed = (o: any): string => colors.bgRed(o.toString());
export const bgGreen = (o: any): string => colors.bgGreen(o.toString());
export const bgYellow = (o: any): string => colors.bgYellow(o.toString());
export const bgBlue = (o: any): string => colors.bgBlue(o.toString());
export const bgMagenta = (o: any): string => colors.bgMagenta(o.toString());
export const bgCyan = (o: any): string => colors.bgCyan(o.toString());
export const bgWhite = (o: any): string => colors.bgWhite(o.toString());
export const bold = (o: any): string => colors.bold(o.toString());
export const dim = (o: any): string => colors.dim(o.toString());
export const italic = (o: any): string => colors.italic(o.toString());
export const under = (o: any): string => colors.underline(o.toString());
